apiVersion: v1
kind: PersistentVolumeClaim
metadata:
      name: prom-pvc
      labels:
              env: production
spec:
      accessModes:
              - ReadWriteOnce
      resources:
              requests:
                      storage: 1Gi
